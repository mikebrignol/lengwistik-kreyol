# Lengwistik Kreyòl

A React and Node.js starter application.

## Run locally

1. Copy `.env.example` to `.env` if you need to change the API port.
2. Run `npm install`.
3. Run `npm run dev`.

The React app runs at `http://localhost:5173` and the API at `http://localhost:3001`.
